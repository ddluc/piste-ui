import styled, { Theme } from 'styled-components';

// Example component Props
type DefaultTabProps = {

};

// Defined a styled component implmementation
export const DefaultTab = styled.div<DefaultTabProps>`
    padding-left: 15px;
    padding-right: 15px;
`;
