import styled from 'styled-components';
import { px } from '../../../util';

type Props = {};

export const ButtonIcon = styled.span<Props>`
  display: flex; 
  align-self: center;
  height: 16px; 
  width: 16px; 
`;
